# SuperDev UI 优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 对 SuperDev macOS App 的 4 个 SwiftUI 文件做 6 项 UI 体验和视觉一致性优化。

**Architecture:** 全部改动限定在现有文件内，不引入新文件。LogStore 新增一个只读查询方法供 PopoverView 使用；LogPanelView 新增两个 @State 变量管理滚动跟随状态；其余改动均为纯样式替换。

**Tech Stack:** SwiftUI (macOS 26.3+), GRDB (SQLite), AppKit (NSPasteboard)

---

## 文件改动汇总

| 文件 | 改动内容 |
|------|---------|
| `SuperDev/UI/MainWindow/LogPanelView.swift` | 优化 1（浮动徽章）、2（颜色统一）、3（状态栏 badge）、5（右键复制） |
| `SuperDev/UI/Settings/SettingsView.swift` | 优化 4（深色重写） |
| `SuperDev/AppCore/Log/LogStore.swift` | 优化 6（新增 lastErrorLog 方法） |
| `SuperDev/UI/MenuBar/PopoverView.swift` | 优化 6（退出 tooltip） |

---

### Task 1: 颜色统一 — 将 LogPanelView 硬编码颜色替换为 Theme 常量

**Files:**
- Modify: `SuperDev/UI/MainWindow/LogPanelView.swift`

背景：`logList` 背景、时间戳、日志正文三处使用裸数字颜色，与 Theme 体系脱节。

- [ ] **Step 1: 替换 logList 背景色**

找到 `logList` 计算属性中的：
```swift
.background(Color(red: 0.12, green: 0.12, blue: 0.12))
```
替换为：
```swift
.background(Theme.bgSecondary)
```

- [ ] **Step 2: 替换 logRow 中的时间戳颜色**

找到 `logRow` 方法中的：
```swift
.foregroundColor(Color(white: 0.45))
```
替换为：
```swift
.foregroundColor(Theme.textTertiary)
```

- [ ] **Step 3: 替换 logRow 中的日志正文颜色**

找到 `logRow` 方法中的：
```swift
.foregroundColor(Color(white: 0.85))
```
替换为：
```swift
.foregroundColor(Theme.textPrimary)
```

- [ ] **Step 4: 构建验证**

在 Xcode 中执行 Build（⌘B），确认编译无错误。

- [ ] **Step 5: Commit**

```bash
git add SuperDev/SuperDev/UI/MainWindow/LogPanelView.swift
git commit -m "style: unify LogPanelView colors to Theme constants"
```

---

### Task 2: 状态栏 badge 美化 — 错误/警告计数改为带背景色 badge

**Files:**
- Modify: `SuperDev/UI/MainWindow/LogPanelView.swift`

背景：`statusBar` 中 `3E` `12W` 裸字符串不够直观，改为带颜色背景的 badge。

- [ ] **Step 1: 替换 statusBar 实现**

找到 `statusBar` 计算属性，将整个属性替换为：

```swift
private var statusBar: some View {
    HStack {
        let s = stats
        Text("共 \(s.total) 条 · 已折叠 \(s.folded) 条")
            .font(.caption)
            .foregroundColor(.secondary)
        Spacer()
        if s.errors > 0 {
            statusBadge("● \(s.errors) 错误", color: Theme.statusFailed)
        }
        if s.warns > 0 {
            statusBadge("● \(s.warns) 警告", color: Theme.statusStarting)
        }
    }
    .padding(.horizontal, 12)
    .padding(.vertical, 4)
    .background(Color(NSColor.controlBackgroundColor))
}

private func statusBadge(_ text: String, color: Color) -> some View {
    Text(text)
        .font(.system(size: 9))
        .foregroundColor(color)
        .padding(.horizontal, 7)
        .padding(.vertical, 1)
        .background(color.opacity(0.1))
        .overlay(RoundedRectangle(cornerRadius: 4).stroke(color.opacity(0.2), lineWidth: 1))
        .cornerRadius(4)
}
```

- [ ] **Step 2: 构建验证**

在 Xcode 中执行 Build（⌘B），确认编译无错误。

- [ ] **Step 3: Commit**

```bash
git add SuperDev/SuperDev/UI/MainWindow/LogPanelView.swift
git commit -m "style: improve status bar error/warn badges with color backgrounds"
```

---

### Task 3: 日志行右键复制 — 添加 contextMenu

**Files:**
- Modify: `SuperDev/UI/MainWindow/LogPanelView.swift`

背景：日志行目前只能文字选中，缺少快速复制整行或消息的能力。

- [ ] **Step 1: 在 logRow 末尾添加 contextMenu modifier**

找到 `logRow` 方法，在最后的 `.cornerRadius(2)` 后添加：

```swift
.contextMenu {
    Button {
        let formatted = "\(entry.timestamp.formatted(.dateTime.hour().minute().second())) [\(entry.serviceName)] \(entry.level.rawValue) \(entry.message)"
        NSPasteboard.general.clearContents()
        NSPasteboard.general.setString(formatted, forType: .string)
    } label: {
        Label("复制此行", systemImage: "doc.on.doc")
    }

    Button {
        NSPasteboard.general.clearContents()
        NSPasteboard.general.setString(entry.message, forType: .string)
    } label: {
        Label("复制消息", systemImage: "text.quote")
    }
}
```

- [ ] **Step 2: 构建验证**

在 Xcode 中执行 Build（⌘B），确认编译无错误。

- [ ] **Step 3: Commit**

```bash
git add SuperDev/SuperDev/UI/MainWindow/LogPanelView.swift
git commit -m "feat: add right-click context menu to copy log row or message"
```

---

### Task 4: 日志自动跟随 — 浮动「新日志」徽章

**Files:**
- Modify: `SuperDev/UI/MainWindow/LogPanelView.swift`

背景：当前 `onChange(of: filteredLogs.count)` 总是强制滚底，用户查历史时会被打断。改为检测滚动位置，用户上翻时停止自动跟随，右下角浮层显示新增条数。

- [ ] **Step 1: 添加两个 @State 变量**

在 `LogPanelView` 的现有 `@State` 声明区域后添加：

```swift
@State private var isFollowing: Bool = true
@State private var newLogCount: Int = 0
```

- [ ] **Step 2: 替换 logList 计算属性**

将整个 `logList` 计算属性替换为：

```swift
private var logList: some View {
    ScrollViewReader { proxy in
        ScrollView {
            LazyVStack(alignment: .leading, spacing: 0) {
                ForEach(filteredLogs) { entry in
                    logRow(entry)
                        .id(entry.id)
                }
            }
            .padding(.horizontal, 8)
            .padding(.vertical, 4)
        }
        .background(Theme.bgSecondary)
        .onScrollGeometryChange(for: CGFloat.self) { geo in
            geo.contentSize.height - geo.containerSize.height - geo.contentOffset.y
        } action: { _, distanceFromBottom in
            let wasFollowing = isFollowing
            isFollowing = distanceFromBottom < 50
            if isFollowing {
                newLogCount = 0
            }
            if isFollowing && !wasFollowing {
                if let last = filteredLogs.last {
                    withAnimation { proxy.scrollTo(last.id, anchor: .bottom) }
                }
            }
        }
        .onChange(of: filteredLogs.count) { _, _ in
            if isFollowing {
                newLogCount = 0
                if let last = filteredLogs.last {
                    withAnimation { proxy.scrollTo(last.id, anchor: .bottom) }
                }
            } else {
                newLogCount += 1
            }
        }
        .overlay(alignment: .bottomTrailing) {
            if !isFollowing && newLogCount > 0 {
                Button {
                    isFollowing = true
                    newLogCount = 0
                    if let last = filteredLogs.last {
                        withAnimation { proxy.scrollTo(last.id, anchor: .bottom) }
                    }
                } label: {
                    Text("↓ \(newLogCount) 条新日志")
                        .font(.system(size: 11, weight: .medium))
                        .foregroundColor(.white)
                        .padding(.horizontal, 12)
                        .padding(.vertical, 6)
                        .background(Theme.accent)
                        .clipShape(Capsule())
                        .shadow(color: .black.opacity(0.3), radius: 4, y: 2)
                }
                .buttonStyle(.plain)
                .padding(.trailing, 12)
                .padding(.bottom, 8)
                .transition(.opacity.combined(with: .scale(scale: 0.9)))
            }
        }
        .animation(.easeInOut(duration: 0.2), value: !isFollowing && newLogCount > 0)
    }
}
```

- [ ] **Step 3: 构建验证**

在 Xcode 中执行 Build（⌘B），确认编译无错误。

- [ ] **Step 4: Commit**

```bash
git add SuperDev/SuperDev/UI/MainWindow/LogPanelView.swift
git commit -m "feat: add floating new-log badge with smart auto-follow behavior"
```

---

### Task 5: SettingsView 深色重写

**Files:**
- Modify: `SuperDev/UI/Settings/SettingsView.swift`

背景：SettingsView 使用系统默认白色控件，与整体深色主题脱节。

- [ ] **Step 1: 替换整个 SettingsView 实现**

将 `SettingsView.swift` 的全部内容替换为：

```swift
import SwiftUI

struct SettingsView: View {
    @EnvironmentObject var core: AppCore
    @State private var showAddProject = false

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            projectList
            Divider()
                .background(Theme.borderPrimary)
            addButton
        }
        .background(Theme.bgPrimary)
        .frame(width: 480, height: 320)
        .sheet(isPresented: $showAddProject) {
            AddProjectView().environmentObject(core)
        }
    }

    private var projectList: some View {
        ScrollView {
            VStack(spacing: 1) {
                ForEach(core.projects) { project in
                    projectRow(project)
                }
            }
            .padding(8)
        }
    }

    private func projectRow(_ project: Project) -> some View {
        HStack(spacing: 12) {
            VStack(alignment: .leading, spacing: 3) {
                Text(project.name)
                    .fontWeight(.medium)
                    .foregroundColor(Theme.textPrimary)
                Text(project.rootPath)
                    .font(.caption)
                    .foregroundColor(Theme.textSecondary)
                    .lineLimit(1)
                    .truncationMode(.middle)
            }
            Spacer()
            Button {
                try? core.reloadConfig(for: project)
            } label: {
                Image(systemName: "arrow.clockwise")
                    .foregroundColor(Theme.textSecondary)
            }
            .buttonStyle(.plain)
            .help("重新加载配置")

            Button(role: .destructive) {
                core.removeProject(project)
            } label: {
                Image(systemName: "trash")
                    .foregroundColor(Theme.statusFailed)
            }
            .buttonStyle(.plain)
        }
        .padding(.horizontal, 12)
        .padding(.vertical, 10)
        .background(Theme.bgElevated)
        .cornerRadius(6)
    }

    private var addButton: some View {
        HStack {
            Spacer()
            Button {
                showAddProject = true
            } label: {
                HStack(spacing: 6) {
                    Image(systemName: "plus")
                    Text("添加项目")
                }
                .font(.system(size: 12, weight: .medium))
                .foregroundColor(.white)
                .padding(.horizontal, 14)
                .padding(.vertical, 7)
                .background(Theme.accent)
                .cornerRadius(6)
            }
            .buttonStyle(.plain)
            .padding(12)
        }
        .background(Theme.bgPrimary)
    }
}
```

- [ ] **Step 2: 构建验证**

在 Xcode 中执行 Build（⌘B），确认编译无错误。

- [ ] **Step 3: Commit**

```bash
git add SuperDev/SuperDev/UI/Settings/SettingsView.swift
git commit -m "style: rewrite SettingsView with dark Theme colors"
```

---

### Task 6: 服务退出 Tooltip — LogStore 新增方法 + PopoverView 接入

**Files:**
- Modify: `SuperDev/AppCore/Log/LogStore.swift`
- Modify: `SuperDev/UI/MenuBar/PopoverView.swift`

背景：服务状态显示「已退出」时没有错误上下文，鼠标悬停应显示最后一条错误日志。

- [ ] **Step 1: 在 LogStore 新增 lastErrorLog 方法**

在 `LogStore.swift` 的 `deleteOldRuns` 方法之后、`// MARK: - Private` 注释之前添加：

```swift
// lastErrorLog 返回指定服务最后一条 error 或 unknown 级别的日志。
//
// 参数：
//   - serviceId: 服务 ID
//
// 返回：最后一条错误日志，若无则为 nil
func lastErrorLog(for serviceId: UUID) -> LogEntry? {
    fetch(serviceId: serviceId, levels: [.error, .unknown], limit: 1).first
}
```

- [ ] **Step 2: 在 AppCore 暴露 lastErrorLog 方法**

在 `AppCore.swift` 的 `filteredLogs` 方法之后添加：

```swift
func lastErrorLog(for serviceId: UUID) -> LogEntry? {
    logStore.lastErrorLog(for: serviceId)
}
```

- [ ] **Step 3: 在 PopoverView 的 serviceRow 添加 tooltip**

找到 `serviceRow` 方法中显示状态文字的这一行：

```swift
Text(statusLabel(service.status))
    .font(.system(size: 9))
    .foregroundColor(serviceStatusColor(service.status))
```

替换为：

```swift
Text(statusLabel(service.status))
    .font(.system(size: 9))
    .foregroundColor(serviceStatusColor(service.status))
    .help(failureTooltip(for: service))
```

然后在 `PopoverView` 的 `// MARK: - Helpers` 区域添加：

```swift
private func failureTooltip(for service: Service) -> String {
    guard service.status == .failed,
          let entry = core.lastErrorLog(for: service.id) else { return "" }
    let msg = entry.message
    return msg.count > 120 ? String(msg.prefix(120)) + "…" : msg
}
```

- [ ] **Step 4: 构建验证**

在 Xcode 中执行 Build（⌘B），确认编译无错误。

- [ ] **Step 5: Commit**

```bash
git add SuperDev/SuperDev/AppCore/Log/LogStore.swift
git add SuperDev/SuperDev/AppCore/AppCore.swift
git add SuperDev/SuperDev/UI/MenuBar/PopoverView.swift
git commit -m "feat: show last error log as tooltip on failed service status"
```

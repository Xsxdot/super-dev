{
  "responsibility": "Describe the isolated Python Fixture Protocol v1 process, provider configuration and debug gates.",
  "boundary": "This descriptor contributes provider evidence only, owns zero MCP rows, and never permits a python3 shim.",
  "schema_version": 1,
  "kind": "superdev.windows-validation.fixture",
  "id": "python",
  "contract_version": "v1",
  "provider": "python",
  "cwd": "fixtures/python",
  "build": {"windows_command": "build.cmd", "prerequisites": ["CPython 3.14.6 x64", "pip", "debugpy 1.8.21 wheel or opt-in network install"], "windows_entry": "build.cmd", "working_directory": ".", "success_exit_code": 0, "blocked_exit_code": 10},
  "run": {"windows_command": "run.cmd", "port": 18172, "windows_entry": "run.cmd", "working_directory": ".", "default_port": 18172, "clean_stop": ["CTRL_C", "CTRL_BREAK"]},
  "readiness": {
    "url": "http://127.0.0.1:18172/healthz",
    "status": 200,
    "method": "GET",
    "path": "/healthz",
    "expected_status": 200,
    "expected_fields": {"ready": true, "contract_version": "v1", "provider": "python"}
  },
  "probe": {"method": "POST", "path": "/api/probe", "authentication": "bearer_campaign_id", "normal_status": 200, "controlled_error_status": 500},
  "language_runtime": {
    "cwd": ".",
    "env_path_prepend": ".venv\\Scripts",
    "config": {"program": "src/server.py"}
  },
  "runtime": {
    "config": {"program": "src/server.py"},
    "env": {"FIXTURE_PORT": "18172", "FIXTURE_CAMPAIGN_ID": "{{campaign_id}}", "PATH_PREPEND": ".venv\\Scripts"}
  },
  "debug": {
    "strategy": "prearm-listen",
    "adapter": "python",
    "preflight": ["preflight.cmd", "debug"],
    "source": "src/server.py",
    "line": 55,
    "variables": ["fixture_marker", "fixture_count", "fixture_provider"],
    "prerequisites": ["debugpy 1.8.21 in fixture venv", "product python3 command resolves without a shim"],
    "marker": "SUPERDEV_FIXTURE_BREAKPOINT",
    "expected_non_secret_variables": {"fixture_marker": "breakpoint-visible", "fixture_count": 42, "fixture_provider": "python"},
    "product_command_contract": "python3 -m debugpy.adapter --host 127.0.0.1 --port <port>",
    "missing_python3_classification": "product_adapter_contract_defect"
  },
  "prerequisites": [
    {"name": "CPython", "version": "3.14.6", "required_for": ["build", "run", "debug"]},
    {"name": "debugpy", "version": "1.8.21", "location": ".venv", "required_for": ["run_start_dev", "debug"]},
    {"name": "python3 command", "version": "resolves to debugpy 1.8.21 environment", "required_for": ["debug_launch"], "missing_verdict": "PRODUCT_DEFECT"}
  ],
  "contract": {
    "normal_path": "/api/probe",
    "error_path": "/api/probe",
    "auth_header": "Authorization",
    "auth_value_env": "FIXTURE_CAMPAIGN_ID",
    "auth_scheme": "bearer_campaign_id",
    "authorization_template": "Bearer superdev-validation-{campaign_id}"
  },
  "coverage": {"provider_report": true, "mcp_tool_rows": []}
}
